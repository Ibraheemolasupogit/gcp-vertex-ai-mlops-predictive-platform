# Deployment Evidence

Add real deployment screenshots and command evidence here in later milestones.
Do not add credentials, secrets, service account keys, or unredacted sensitive
values.

Recommended R5 evidence:

- `env.example` copied to a local uncommitted environment file with placeholders replaced.
- Artifact Registry repository created.
- Docker image pushed to Artifact Registry.
- Cloud Run service deployed.
- Cloud Run service URL captured.
- `/health` endpoint response screenshot.
- `/predict` endpoint response screenshot.
- Cloud Logging entry screenshot.
- Cloud Monitoring chart screenshot.
- Cleanup command output if resources are removed after demonstration.

Do not add fake screenshots.
