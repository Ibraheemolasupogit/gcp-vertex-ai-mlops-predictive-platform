# Vertex AI Custom Training And Model Registry Evidence

Add real screenshots or command outputs here only after a real Vertex AI
demonstration has been performed. Do not add fake screenshots, credentials,
service account keys, or unredacted project identifiers.

Evidence to capture later:

- Vertex AI custom training job screen.
- Custom training job logs.
- Cloud Storage model artifact output.
- Vertex AI Model Registry model entry.
- Model version details and version alias.
- Model metadata and labels.
- Metrics evidence linked to `outputs/model_metrics.json`.
- Approval or lifecycle mapping linked to deployment gate outputs.
- Link back to a local model card or evaluation report if one is added later.

R9 only prepares the mapping. Vertex AI endpoint deployment, online prediction,
and batch prediction are deferred to R10.
