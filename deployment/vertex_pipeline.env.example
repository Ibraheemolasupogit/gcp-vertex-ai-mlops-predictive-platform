# Vertex AI Pipelines / Kubeflow placeholders.
# Copy into a local, uncommitted environment file only when preparing a real
# pipeline demonstration. Do not commit real project IDs, bucket names,
# credentials, or service account details.

PROJECT_ID=your-gcp-project-id
REGION=europe-west2
PIPELINE_ROOT=gs://your-pipeline-root-bucket/pipelines
VERTEX_STAGING_BUCKET=gs://your-vertex-staging-bucket
PIPELINE_NAME=predictive-maintenance-mlops-pipeline
FEATURE_TABLE_URI=gs://your-bucket/data/feature_table.csv
MODEL_OUTPUT_URI=gs://your-bucket/models/predictive-maintenance/
MODEL_DISPLAY_NAME=predictive-maintenance-model
APPROVAL_THRESHOLD_F1=0.80
APPROVAL_THRESHOLD_RECALL=0.80
SERVICE_ACCOUNT=your-pipeline-service-account@your-project.iam.gserviceaccount.com
