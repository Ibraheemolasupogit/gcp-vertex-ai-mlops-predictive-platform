# Distributed Systems Design

The future cloud design should treat data processing, model training, batch
prediction, monitoring, and serving as separate workloads with clear contracts.
This keeps failure handling, retries, observability, and scaling decisions
independent.

Milestone 1 captures these boundaries through repository structure only.
