# Model Lifecycle

The planned model lifecycle begins with local baseline training, evaluation,
and artifact capture. Candidate models will pass through deployment gates before
being promoted in a local registry and, later, Vertex AI Model Registry.

Milestone 1 does not define model algorithms or train models.
